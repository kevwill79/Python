#List and the enumerate function

#Using the enumerate function to iterate over an iterable
alpha_list = []
for letter in enumerate("abcd"):
    alpha_list.append(letter)
    
print(alpha_list)